{
  "name": "focus-forge-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Separate mobile wrapper for Focus Forge",
  "scripts": {
    "sync:web": "node scripts/copy-web.js",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "android:build": "npm run sync:web && npx cap sync android && node scripts/build-android.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
